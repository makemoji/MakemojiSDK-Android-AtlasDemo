<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.layer.messenger"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- QR Code, CameraSender -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- GallerySender -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- LocationSender -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Layer SDK -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.GET_TASKS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE"/>

    <permission
        android:name="com.layer.messenger.permission.LAYER_PUSH"
        android:protectionLevel="signature"/>
    <uses-permission android:name="com.layer.messenger.permission.LAYER_PUSH"/>
    <permission
        android:name="com.layer.messenger.permission.C2D_MESSAGE"
        android:protectionLevel="signature"/>
    <uses-permission android:name="com.layer.messenger.permission.C2D_MESSAGE"/>

    <application
        android:name="com.layer.messenger.App"
        android:allowBackup="true"
        android:icon="@drawable/layer_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/AppTheme"
        >

        <!-- Activity: List of Conversations -->
        <activity android:name="com.layer.messenger.ConversationsListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Activity: Messages in a Conversation; AddressBar -->
        <activity
            android:name="com.layer.messenger.MessagesListActivity"
            android:windowSoftInputMode="adjustResize"
            >
        </activity>

        <!-- Activity: Global app settings -->
        <activity
            android:name=".AppSettingsActivity"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <!-- Activity: Individual Conversation settings -->
        <activity
            android:name=".ConversationSettingsActivity"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <!-- Receiver: Our app notification handling -->
        <receiver
            android:name=".PushNotificationReceiver"
            android:permission="com.layer.messenger.permission.LAYER_PUSH">

            <!-- Layer SDK broadcasts this, which we use to generate notifications -->
            <intent-filter>
                <action android:name="com.layer.sdk.PUSH"/>
                <category android:name="com.layer.messenger"/>
            </intent-filter>

            <!-- Our app sets this as a PendingIntent to handle user notification swipe-outs -->
            <intent-filter>
                <action android:name="com.layer.messenger.CANCEL_PUSH"/>
                <category android:name="com.layer.messenger"/>
            </intent-filter>
        </receiver>

        <!-- Receiver: Layer SDK GCM handling -->
        <receiver
            android:name="com.layer.sdk.services.GcmBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter android:priority="470">
                <action android:name="com.google.android.c2dm.intent.RECEIVE"/>
                <action android:name="com.google.android.c2dm.intent.REGISTRATION"/>
                <action android:name="com.google.android.c2dm.intent.REGISTER"/>

                <category android:name="com.layer.messenger"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
